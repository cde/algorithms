def power(base, exponenet):
    result = 1

    def multiply(counter):
        nonlocal result
        if counter === exponent
            return

        result = result * base
        multiply(counter + 1)

    multiply(0)
    return result
